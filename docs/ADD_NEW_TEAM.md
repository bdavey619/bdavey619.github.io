# Add New MLB Team — Morning Brief Checklist

Use this document to add any new team to the Morning Brief system. Replace `<team>` with the lowercase team slug (e.g. `giants`, `mariners`, `athletics`, `dodgers`) and `<TEAM>` with the Python constant name (e.g. `GIANTS`, `MARINERS`).

This document is designed to be handed directly to Claude Code:
> "Use `docs/ADD_NEW_TEAM.md` to add the Giants."

---

## A. Required Team Config Fields

Gather these values before touching any code.

| Field | Type | Notes |
|---|---|---|
| `team_id` | int | MLB Stats API team ID — look up at `statsapi.mlb.com/api/v1/teams?sportId=1` |
| `team_abbr` | str | Short label shown in linescore/scoreboards (e.g. `"SF"`, `"SEA"`) |
| `team_name` | str | Short team name (e.g. `"Giants"`, `"Mariners"`) |
| `team_city` | str | City name (e.g. `"San Francisco"`, `"Seattle"`) |
| `home_venue` | str | Full venue name (e.g. `"Oracle Park"`, `"T-Mobile Park"`) |
| `home_venue_short` | str | Short venue name for subheads (e.g. `"Oracle"`, `"T-Mobile"`) |
| `league_id` | int | `104` = NL, `103` = AL |
| `division_id` | int | See table below |
| `division_name` | str | (e.g. `"NL West"`, `"AL West"`) |
| `division_short` | str | Direction word only: `"West"`, `"East"`, `"Central"` |
| `tz_offset` | int | UTC offset during DST (e.g. `-7` = PT, `-5` = CT, `-4` = ET) |
| `tz_label` | str | Timezone abbreviation (e.g. `"PT"`, `"ET"`) |
| `site_url` | str | Published brief URL (e.g. `"https://bdavey619.github.io/giants/"`) |
| `accent_color` | str | Primary brand hex color (e.g. `"#FD5A1E"` for Giants) |

**Division IDs:**

| Division | ID |
|---|---|
| AL East | 201 |
| AL Central | 202 |
| AL West | 200 |
| NL East | 204 |
| NL Central | 205 |
| NL West | 203 |

**Timezone note:** Use the UTC offset *during the MLB season (DST)*. PT = -7, MT = -6, CT = -5, ET = -4.

**Accent color:** Use the team's primary brand color, not necessarily the dominant jersey color. Dark colors work better for the brief's design aesthetic. Check the team's official style guide.

---

## B. Files and Folders to Create

```
<team>/
├── build_brief.py        (copy from giants/, change import + CFG line)
├── index.html            (copy from giants/, update title + division labels + SCHEDULE_CONFIG)
├── app.js                (copy from giants/)
├── styles.css            (copy from giants/, update accent_color CSS variable)
├── email_template.html   (copy from giants/)
├── archive.html          (copy from giants/, update ARCHIVE_CONFIG block)
├── schedule.html         (copy from giants/, update SCHEDULE_CONFIG block)
├── brief.json            (placeholder — see scaffold below)
├── story_state.json      (placeholder — see scaffold below)
├── hook_history.json     (placeholder — see scaffold below)
├── schedule.json         (generated at runtime — do not hand-author)
└── archive/
    └── index.json        (generated at runtime — do not hand-author)
```

**brief.json placeholder** — keeps `render_email.py` from crashing before the first real run:
```json
{}
```

**story_state.json placeholder:**
```json
{
  "trend": "neutral",
  "driver": "balanced",
  "confidence": "low",
  "pressure": "none",
  "record_w": 0,
  "record_l": 0,
  "streak": "",
  "last10_w": 0,
  "era": 0.0,
  "ops": 0.0,
  "gb": 0.0,
  "division_rank": 0,
  "last_result": "",
  "date": "",
  "game_emotion_level": "normal"
}
```

**hook_history.json placeholder:**
```json
{
  "recent_types": []
}
```

---

## C. Required Data Systems

Every team must support three systems. None are optional.

**1. Daily Brief**
- `<team>/brief.json` — generated by `<team>/build_brief.py`
- Powers the web brief and the daily email

**2. Archive System**
- `<team>/archive/YYYY-MM-DD.json` — one file per published day
- `<team>/archive/index.json` — manifest, generated by `archive_brief.py --team <team>`
- `<team>/archive.html` — renders both list view and detail view via `/js/archive.js`

**3. Schedule System**
- `<team>/schedule.json` — generated by `build_schedule.py --team <team>`
- `<team>/schedule.html` — renders the monthly calendar view via `/js/schedule.js`

**Checklist:**
- [ ] `python archive_brief.py --team <team>` runs successfully
- [ ] `python build_schedule.py --team <team>` runs successfully
- [ ] `<team>/archive.html` loads with at least one entry
- [ ] `<team>/schedule.html` renders the current month

---

## D. Files to Update

### 1. `engine/team_config.py`

Add a new `TeamConfig` constant at the bottom of the file. Follow the exact pattern of `PADRES` and `GIANTS`:

```python
GIANTS = TeamConfig(
    team_id=137,
    team_abbr="SF",
    team_name="Giants",
    team_city="San Francisco",
    home_venue="Oracle Park",
    home_venue_short="Oracle",
    league_id=104,
    division_id=203,
    division_name="NL West",
    division_short="West",
    tz_offset=-7,
    tz_label="PT",
    site_url="https://bdavey619.github.io/giants/",
    accent_color="#FD5A1E",
)
```

### 2. `render_email.py` (root-level)

Two changes:

**Line 23 — update import:**
```python
from engine.team_config import PADRES, YANKEES, GIANTS
```

**Lines 25-28 — update `_TEAM_CONFIGS` dict:**
```python
_TEAM_CONFIGS = {
    "padres":  PADRES,
    "yankees": YANKEES,
    "giants":  GIANTS,
}
```

### 3. `send_email.py` (root-level)

Same two changes as `render_email.py` above (same structure, same lines).

### 4. `<team>/build_brief.py`

Copy `giants/build_brief.py`. Make exactly two changes:

- **Line with `from engine.team_config import`** — add the new team constant to the import
- **Line `CFG = GIANTS`** — change to `CFG = <TEAM>`

No other changes needed. The entire pipeline is config-driven through `CFG`.

### 5. `<team>/index.html`

Copy `giants/index.html`. Update:
- `<title>` tag: `"<TeamName> Morning Brief"`
- `<h1 class="masthead-title">`: `"<TeamName> Morning Brief"`
- Division label in `#summary-bar` (e.g. `<span class="label">NL West</span>`)
- Any hardcoded venue or team references

**Required nav** — must appear between the masthead title and the horizontal rule:

```html
<nav class="brief-nav">
  <a href="archive.html" class="brief-nav-link">Archive</a>
  <a href="schedule.html" class="brief-nav-link">Schedule</a>
</nav>
```

This nav is part of the product UI contract. Missing it = incomplete team setup.

### 6. `<team>/styles.css`

Copy `giants/styles.css`. Update the CSS accent color variable:
```css
--accent: <accent_color>;
```

### 7. `.github/workflows/<team>-brief.yml`

Create a new workflow file (see Section F).

### 8. `<team>/archive.html`

Copy `giants/archive.html`. Update only the `ARCHIVE_CONFIG` block near the bottom of the file:

```html
<script>
  window.ARCHIVE_CONFIG = {
    team:     '<team>',
    teamName: '<TeamName>',
  };
</script>
```

Do not change script paths — `/js/archive.js` is shared across all teams.

### 9. `<team>/schedule.html`

Copy `giants/schedule.html`. Update only the `SCHEDULE_CONFIG` block near the bottom of the file:

```html
<script>
  window.SCHEDULE_CONFIG = {
    team:      '<team>',
    teamName:  '<TeamName>',
    teamAbbr:  '<ABBR>',
    division:  '<Division Name>',
    homeVenue: '<Venue Name>',
    teamClass: '<team>',
  };
</script>
```

Do not change script paths — `/js/schedule.js` is shared across all teams.

### 10. Homepage / Projects page (optional)

The root `index.html` has no team-brief links — no update needed.

The `projects/index.html` has a "Padres Newsletter" entry in "Current Builds" — add a similar entry for the new team if desired.

---

## E. Shared Frontend Dependencies

All teams rely on shared JS files at the root `/js/` directory:

| File | Purpose |
|---|---|
| `/js/archive.js` | Renders archive list + detail view |
| `/js/schedule.js` | Renders monthly calendar |

**`archive.html`** must define `window.ARCHIVE_CONFIG` before the script tag. The script reads team identity from this config object.

**`schedule.html`** must define `window.SCHEDULE_CONFIG` before the script tag.

Neither script accepts command-line arguments or URL parameters — team identity comes entirely from the inline config.

**Checklist:**
- [ ] Script src paths use `../js/archive.js` and `../js/schedule.js` (relative from `<team>/`)
- [ ] Config variable names match exactly: `ARCHIVE_CONFIG`, `SCHEDULE_CONFIG`
- [ ] `teamClass` in `SCHEDULE_CONFIG` matches the team slug in `styles.css`
- [ ] No cross-team style leakage — open all three team schedule pages and confirm no shared-class collisions

---

## F. Workflow File

Create `.github/workflows/<team>-brief.yml`. Copy `giants-brief.yml` and make these changes.

**Step order — order matters:**
1. `build_brief.py` — generate today's brief
2. `build_schedule.py` — update `schedule.json` (must run before archive)
3. `archive_brief.py` — archive today's brief (reads schedule; must run before commit)
4. `render_email.py` — render email HTML
5. `send_email.py` — send the email
6. Commit and push all generated files

```yaml
name: <TeamName> Morning Brief

on:
  schedule:
    # <description of local time>
    # Stagger at least 35 minutes from other team workflows to avoid resource contention.
    - cron: "<minute> <hour> * * *"
  workflow_dispatch:

jobs:
  build-brief:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install requests

      - name: Generate brief.json
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python <team>/build_brief.py

      - name: Build schedule
        run: python build_schedule.py --team <team>

      - name: Archive brief
        run: python archive_brief.py --team <team>

      - name: Render email HTML
        run: python render_email.py --team <team>

      - name: Send email
        env:
          RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
          EMAIL_TO: ${{ secrets.<TEAM>_EMAIL_TO }}
          EMAIL_FROM: ${{ secrets.<TEAM>_EMAIL_FROM }}
        run: python send_email.py --team <team>

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add <team>/brief.json <team>/story_state.json <team>/hook_history.json \
                  <team>/email_preview.html <team>/schedule.json <team>/archive/
          if git diff --cached --quiet; then
            echo "No changes — skipping commit."
          else
            git commit -m "chore: update <TeamName> morning brief [skip ci]"
            git pull --rebase origin main
            git push
          fi
```

**Current cron schedule (avoid these slots):**
- Padres: `25 10 * * *` (6:25 AM ET)
- Yankees: `0 11 * * *` (7:00 AM ET)
- Giants: `35 11 * * *` (7:35 AM ET)

Space new teams at least 35 minutes apart from any existing slot.

---

## G. Secrets Checklist

Add secrets under **Settings → Secrets and variables → Actions**.

**Key strategy:**
- `RESEND_API_KEY` (global, already exists) is sufficient for all teams
- `<TEAM>_RESEND_API_KEY` is an optional per-team override — only set if you need a separate Resend account for this team
- `send_email.py` prefers the team-specific key and falls back to the global key automatically

**Required secrets per team:**

| Secret name | Description |
|---|---|
| `<TEAM>_EMAIL_TO` | Recipient address(es), comma-separated |
| `<TEAM>_EMAIL_FROM` | Verified sender address (e.g. `brief@yourdomain.com`) |

**Already present — do not duplicate:**

| Secret name | Description |
|---|---|
| `ANTHROPIC_API_KEY` | Shared across all teams |
| `RESEND_API_KEY` | Global Resend key, fallback for all teams |

**Optional per-team override:**

| Secret name | Description |
|---|---|
| `<TEAM>_RESEND_API_KEY` | Per-team Resend key — only if using a separate sender account |

**Checklist:**
- [ ] `RESEND_API_KEY` (global) exists in repo secrets
- [ ] `<TEAM>_EMAIL_FROM` is a verified domain or address in Resend
- [ ] `<TEAM>_EMAIL_TO` is set

**Notes:**
- `<TEAM>_EMAIL_FROM` must be verified in Resend before the first scheduled run. If not verified, the send step returns a 403 or 422.
- Padres uses `RESEND_API_KEY` / `EMAIL_TO` / `EMAIL_FROM` (no team prefix) — a historical artifact. All new teams use the `<TEAM>_` prefix pattern.

---

## H. Local Validation Commands

Run these in order after setting up all files:

```bash
# 1. Generate brief.json (requires ANTHROPIC_API_KEY in env for AI narrative;
#    falls back to deterministic insight if the key is absent)
python <team>/build_brief.py

# 2. Build schedule.json
python build_schedule.py --team <team>

# 3. Archive today's brief
python archive_brief.py --team <team>

# 4. Render the email preview
python render_email.py --team <team>

# 5. Open the rendered email in a browser
open <team>/email_preview.html

# 6. Open the web brief
open <team>/index.html

# 7. Open the archive page
open <team>/archive.html

# 8. Open the schedule page
open <team>/schedule.html
```

If `brief.json` is empty (placeholder), steps 2–4 will fail — run step 1 first.

---

## I. Website Validation Checklist

After opening `<team>/index.html`:

- [ ] Page title is correct: `"<TeamName> Morning Brief"`
- [ ] Masthead title is correct: `"<TeamName> Morning Brief"`
- [ ] Division label in summary bar matches the team's actual division (e.g. `NL West`, `AL West`)
- [ ] Standings show the correct division — not a leftover division from the copied team
- [ ] The team's own row in the standings table is highlighted
- [ ] State of Play section renders with text (not blank)
- [ ] Game Driver block only appears when `confidence` is `"high"` or `"medium"`
- [ ] Turning Point block only appears when `confidence` is `"high"`
- [ ] `story_hook` appears in the subhead only when meaningful (not on off days or data failures)
- [ ] No debug metadata is visible (no raw JSON, no error messages)
- [ ] Accent color in the result badge matches `<accent_color>` in team config
- [ ] Hard refresh (`Cmd+Shift+R` / `Ctrl+Shift+R`) after any `app.js` or `styles.css` change — browser caches these aggressively
- [ ] Nav (Archive / Schedule) appears between masthead title and horizontal rule
- [ ] Archive link opens `<team>/archive.html` and shows entries
- [ ] Schedule link opens `<team>/schedule.html` and renders the current month

---

## J. Email Validation Checklist

After opening `<team>/email_preview.html`:

- [ ] Subject line will render as: `"<TeamName> Morning Brief — <Month Day>"`
- [ ] CTA link (`site_url`) points to the correct team page (not padres or yankees)
- [ ] Accent color on the WIN/LOSS badge matches the team's `accent_color`
- [ ] Highlights link appears only when a highlights URL is present
- [ ] State of Play headline and detail text render (not blank)
- [ ] "What to Watch" section appears only when AI narrative is available
- [ ] Game Driver block renders only when `confidence` is `"high"` or `"medium"`, and only when the player differs from the Turning Point player
- [ ] Turning Point block renders only when `confidence` is `"high"`
- [ ] No Padres, Giants, or Yankees leftover text in the template (venue names, division names, color references)
- [ ] Footer `generated_at` shows today's date

---

## K. Narrative / Voice

### K.1 Team Identity Snapshot (complete before touching `engine/narrative.py`)

Define these before writing a single line of voice code. The snapshot ensures the voice profile is intentional, not reactive to one game's result.

| Field | Your answer |
|---|---|
| **Identity** | e.g. "scrappy underdog", "high-expectation contender", "volatile rebuild" |
| **What they trust** | e.g. pitching, stars, depth, chaos |
| **What they don't trust** | e.g. bullpen, offense consistency, closing games |
| **Default emotional tone** | e.g. skeptical, confident, uneasy, opportunistic |
| **One-line framing** | e.g. "They don't have much. They have enough." |

Fill in all five fields for `<TeamName>` before proceeding to K.2.

### K.2 Voice Checklist

**Voice profile is required.** A team without a distinct voice produces generic output indistinguishable from any other team.

- [ ] Voice profile added in `engine/narrative.py`
- [ ] Profile includes: tone, phrasing tendencies, and any team-specific bias (e.g. park factors, roster philosophy, fan expectations)
- [ ] Passes the "does this feel like this team?" test — read 2–3 outputs and ask whether they could apply to any other team
- [ ] Division rivalry context is correct for this team's actual division (Giants → NL West rivals: Dodgers, Padres — not AL East)
- [ ] No leftover Padres, Giants, or Yankees phrasing in narrative output
- [ ] Player arc references are based on actual game data, not forced story templates
- [ ] Review 2–3 consecutive days of narrative output before adding subscribers

**Note:** Voice should influence wording subtly — 1–2 word choices per paragraph. It is not a writing style gimmick. Avoid transplanting clichés ("East Coast grit", "West Coast swagger") — let the actual game story carry the meaning.

---

## L. Common Gotchas

These are issues that have already occurred with Padres/Yankees/Giants builds:

**`app.js` / `styles.css` appear unchanged in browser**
Hard refresh required after any frontend edit: `Cmd+Shift+R` (Mac) or `Ctrl+Shift+R`. The browser caches these files aggressively.

**Workflow push rejected**
The workflow commit step runs `git push` after `git commit`. If another workflow ran simultaneously and pushed first, this will fail with a non-fast-forward error. Fix: add `git pull --rebase origin main` before `git push` in the workflow (already included in the template above).

**Rebase fails — unstaged generated files block it**
If `brief.json` or `story_state.json` have uncommitted local changes, `git pull --rebase` will refuse to run. Fix: add `git restore .` before the rebase to discard any uncommitted generated files:
```yaml
git restore .
git pull --rebase origin main
git push
```

**`brief.json` conflict between local build and cron**
Running `build_brief.py` locally while the cron is active creates a race condition: both write `brief.json`, both try to commit. The cron wins; your local version is overwritten on next pull. Run local builds outside the cron window, or use `workflow_dispatch` to trigger a fresh run instead.

**Missing `ANTHROPIC_API_KEY` in local env**
`build_brief.py` falls back to deterministic insight when the API key is absent — the brief still generates, but `narrative` fields will be empty. The email render falls back gracefully to `insight` fields. This is expected behavior for local testing.

**`story_state.json` and `hook_history.json` must be team-specific**
Both files track per-team narrative state. They must live in `<team>/` and must never be shared across teams. If either file is missing, the narrative engine will initialize them fresh on the next run — this is safe.

**Do not commit `.claude/` files or `.DS_Store`**
The padres directory contains a `.claude/settings.local.json` file. This should never be committed. Check `.gitignore` before the first push for the new team directory.

**Resend sender must be verified before first scheduled run**
If `EMAIL_FROM` is not a verified Resend domain, the send step will return a 403 or 422. Verify the domain in Resend before enabling the cron.

---

## M. Design Principles

Do not add features that do not improve clarity of the season story.

**Guiding question:** "Does this help someone understand the season faster?"

If the answer is not clearly yes → do not add it.

**Examples of features that were considered and rejected:**

| Feature | Reason rejected |
|---|---|
| Series count labels ("· 3 games") | Added visual noise without adding insight |
| Overly dense result labels | Competed with score readability |
| MLB Savant-style visualizations | Complexity without proportional clarity gain |

**What belongs in the brief:**
- Game result (W/L, score)
- Opponent and home/away context
- One key narrative moment or trend
- Standings context
- Next game

**What does not belong:**
- Cumulative stats as the primary content
- Features that require a tooltip to understand
- Anything that makes the page feel like a dashboard

---

## N. Final Go/No-Go Checklist

Complete all items before merging the new team branch or enabling the scheduled workflow:

**Code**
- [ ] `engine/team_config.py` — new constant added, all 14 fields correct
- [ ] `render_email.py` — import updated, `_TEAM_CONFIGS` updated
- [ ] `send_email.py` — import updated, `_TEAM_CONFIGS` updated
- [ ] `<team>/build_brief.py` — import and `CFG` line updated
- [ ] `<team>/index.html` — title, masthead, division label, and `brief-nav` updated
- [ ] `<team>/styles.css` — accent color updated
- [ ] `<team>/archive.html` — `ARCHIVE_CONFIG` updated
- [ ] `<team>/schedule.html` — `SCHEDULE_CONFIG` updated

**Data files**
- [ ] `<team>/brief.json` — placeholder or real data present
- [ ] `<team>/story_state.json` — placeholder present
- [ ] `<team>/hook_history.json` — placeholder present

**Infrastructure**
- [ ] `.github/workflows/<team>-brief.yml` — created with correct step order, cron slot clear of conflicts
- [ ] `RESEND_API_KEY` (global) exists in repo secrets
- [ ] `<TEAM>_EMAIL_TO` set
- [ ] `<TEAM>_EMAIL_FROM` set and verified in Resend

**Local validation**
- [ ] `python <team>/build_brief.py` runs without error
- [ ] `python build_schedule.py --team <team>` runs without error
- [ ] `python archive_brief.py --team <team>` runs without error
- [ ] `python render_email.py --team <team>` runs without error
- [ ] Web brief opens and renders correctly (Section I checklist complete)
- [ ] Email preview opens and renders correctly (Section J checklist complete)

**Narrative quality**
- [ ] Team Identity Snapshot (K.1) completed before writing voice profile
- [ ] Voice profile reviewed (Section K.2 checklist complete)
- [ ] 2–3 days of output reviewed before adding subscribers

**Commit hygiene**
- [ ] No `.claude/` files staged
- [ ] No `.DS_Store` files staged
- [ ] `brief.json` / `story_state.json` / `hook_history.json` are placeholder versions in the initial commit (not a real day's output)

---

## O. Definition of Done

A team is complete when all of the following are true:

- [ ] Email sends successfully on a scheduled run (not just `workflow_dispatch`)
- [ ] Archive page shows entries (at least one day of real history)
- [ ] Schedule renders the current month correctly
- [ ] Wins and losses are visually clear at a glance (left border treatment, W/L badge)
- [ ] Voice feels distinct from other teams — output does not read as generic
- [ ] No references to other teams appear anywhere in the output or UI
- [ ] Navigation (Archive / Schedule) is present and functional on `index.html`

---

## Reference: Quick-Start Command Sequence

```bash
# After all files are created and updated:

# Validate locally
python <team>/build_brief.py
python build_schedule.py --team <team>
python archive_brief.py --team <team>
python render_email.py --team <team>
open <team>/email_preview.html
open <team>/index.html
open <team>/archive.html
open <team>/schedule.html

# Commit the new team
git add engine/team_config.py
git add render_email.py send_email.py
git add <team>/
git add .github/workflows/<team>-brief.yml
git status  # verify nothing unexpected is staged
git commit -m "feat: add <TeamName> morning brief"
git push
```

After the first successful push, trigger a test run manually via **GitHub → Actions → `<TeamName> Morning Brief` → Run workflow** before relying on the cron.
